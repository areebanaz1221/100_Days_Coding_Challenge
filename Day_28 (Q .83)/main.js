"use strict";
function convertCase(str) {
    let upperStr = str.toUpperCase();
    let lowerStr = str.toLowerCase();
    console.log("Uppercase:", upperStr, "Lowercase:", lowerStr);
}
convertCase("Laughter is the best policy!");
