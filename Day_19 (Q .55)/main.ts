// Starts with a list of numbers
let numbers = [7, 6, 8, 9, 10, 12];

// Doubles each number
let doubledNumbers = numbers.map(number => number * 2);

// Shows the new list of doubled numbers
console.log(doubledNumbers); // Output: [ 14, 12, 16, 18, 20, 24 ]
